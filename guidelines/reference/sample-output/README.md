# sample-output/

Representative pipeline OUTPUT, kept for inspection — what a writer produces under the current (post-migration, per-rule) corpus. This directory is NOT part of the samples doctrine: the calibration exemplars are the `docs/sound/` pages named in `guidelines/passes/00-prep.md` (the frozen samples under `guidelines/reference/samples/` were retired as the writer's exemplar on 2026-09-02), the prep pass's exemplar list does not include this directory, and nothing here is a standard to imitate or a source of kernel facts.

| file | what it is |
|---|---|
| `control-route-header.md` | A page written for the migration's writer comparison (`struct tb_cfg_header`, drivers/thunderbolt, tree v7.0 at 028ef9c96e96): written by an Opus campaign writer through the re-pointed passes and the per-rule corpus only, exit suite clean, then audited find-only under the pinned original gates. Status: that audit's findings stand as repair material (adjudications in `progress/cfgheader-rewired/log.md`). The rule revision it was written against is sha-pinned in its dossier HEADER. |
