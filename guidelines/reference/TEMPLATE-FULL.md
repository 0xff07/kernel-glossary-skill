# TOPIC-NAME

> CAUTION: AI-GENERATED CONTENT
>
> STRICTLY DO NOT SUBMIT THIS UPSTREAM UNLESS YOU ARE AN EXPERT INTIMATELY FAMILIAR WITH THIS SUBSYSTEM.

(The lead: what the mechanism is for and where it sits, in words a reader with no context can follow; symbols and counts only after that (WRITING rule 1).)
(ASCII diagram goes here, if any)

## SUMMARY

(Short: the model in prose, the states and transitions, phases or taxonomy the code implements, mapped onto the kernel constructs, and the state set as a member-meaning-construct table with its legal transitions; at most one table and one figure, with member tables and per-request detail in DETAILS beside their excerpts (WRITING rules 2 and 5). A spec-defined model is cited in SPECIFICATIONS; an unspecified model is a disclosed synthesis that names its on-disk sources and keeps every fact under it cited.)

## SPECIFICATIONS

(For a spec-defined topic, cite the normative source of the model here (WRITING rule 2). State that none applies when the subsystem has no external spec.)

## LINUX KERNEL

## KERNEL DOCUMENTATION

## OTHER SOURCES

## REGISTERS

This is meant for the architecture constructs.

The title doesn't have to be "REGISTERS". It can be category-dependent. For example, for ACPI it can be "METHODS" because they play a more important role.

## DETAILS

(Organize this section as a journey or around the model (WRITING rule 2): its subsections are the phases of a process traced in order, or the facets of the model (roles, states, classes), with each cataloged symbol shown inside the phase or facet where it acts, beside its definition and usage excerpts and the paragraph that explains them (WRITING rule 3). Not one section per symbol in catalog order. Every catalog symbol still appears here (parity, PAGE-02).)

